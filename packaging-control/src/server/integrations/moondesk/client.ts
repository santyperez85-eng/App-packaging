export class MoondeskClient {
  async fetchTasks() {
    return {
      connected: false,
      message: "Moondesk integration stub ready. Implement API credentials and mapping in the next phase."
    };
  }
}
