export class SapClient {
  async fetchMaterials() {
    return {
      connected: false,
      message: "SAP integration stub ready. Implement extraction and harmonization in the next phase."
    };
  }
}
